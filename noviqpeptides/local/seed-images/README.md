# Local product photos

Client PNGs used by `wp noviq seed`. Copy the 28 catalog files here (not `Bacwater main.png` or the duplicate `tirz main.png`). Filenames must match `image` in `plugin/data/noviq/products.json`.
