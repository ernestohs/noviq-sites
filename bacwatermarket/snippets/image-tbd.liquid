{%- comment -%}
  Product-media fallback. Bac Water Market uses the original 10 mL catalog
  graphic instead of a gray IMAGE TBD block.
  Accepts:
  - loading: eager | lazy (default lazy)
{%- endcomment -%}
{% render 'bacwater-graphic', loading: loading %}
